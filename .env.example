# Exemple de configuration d'environnement
# Copiez ce fichier en .env et remplissez les valeurs nécessaires

# Flask
SECRET_KEY=dev-secret-key
HOST=0.0.0.0
PORT=8765
FLASK_DEBUG=False

# Dossier de données (pour spotify_tokens.json)
DATA_DIR=./data

# Spotify API: configure via l'endpoint /settings/spotify (stocké dans data/spotify_config.json)
# Optionnel: personnaliser le redirect si différent
SPOTIFY_REDIRECT_URI=http://localhost:8765/spotify/callback
